To run the project you just have to run these commands on the project root folder:

#1 npx react-native init quizApp && mv quizApp/ios quizApp/android ./ && rm -rf quizApp
#2 - yarn install;
    #2.1 (for ios only) - npx pod-install ios
#3 - yarn android;
